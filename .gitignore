# Binaries for programs and plugins
#*.exe
*.exe~
*.dll
*.so
*.dylib
#build/*
#bin/*

src/aws-s3-backup
src/*credentials
src/credentials*
src/credentials
src/processed.txt
src/generated-restore-input*
bin/processed.txt
processed.txt


# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Other stuff
.DS_Store
.vscode
